#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0, i < (int)(n); i++)

int main() {
    int N;
    cin >> N;
    num = N;
    rep(i, N+1){
        cout << num << endl;
        num - 1;
    }
}